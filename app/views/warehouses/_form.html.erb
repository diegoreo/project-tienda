<div class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
  <%= form_with(model: @warehouse, local: true, class: "space-y-6") do |form| %>
    
    <!-- Encabezado del formulario -->
    <div class="bg-gradient-to-r from-teal-600 to-teal-700 text-white px-6 py-4 rounded-lg shadow-md">
      <h2 class="text-2xl font-bold">
        <%= @warehouse.new_record? ? "Nuevo Almacén" : "Editar Almacén" %>
      </h2>
      <p class="text-teal-100 text-sm mt-1">Define las ubicaciones de almacenamiento</p>
    </div>

    <!-- Mensajes de error -->
    <% if @warehouse.errors.any? %>
      <div class="bg-red-50 border-l-4 border-red-500 rounded-lg p-4">
        <div class="flex items-start">
          <svg class="w-5 h-5 text-red-500 mt-0.5 mr-3 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
          </svg>
          <div class="flex-1">
            <h4 class="text-red-800 font-semibold mb-2">
              <%= pluralize(@warehouse.errors.count, "error") %> impiden guardar el almacén:
            </h4>
            <ul class="list-disc list-inside text-red-700 space-y-1">
              <% @warehouse.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        </div>
      </div>
    <% end %>

    <!-- Formulario principal -->
    <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
      <h3 class="text-lg font-semibold text-gray-800 mb-4 flex items-center">
        <svg class="w-5 h-5 mr-2 text-teal-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"/>
        </svg>
        Información del Almacén
      </h3>

      <div class="space-y-5">
        <!-- Nombre del almacén -->
        <div>
          <%= form.label :name, class: "block text-sm font-medium text-gray-700 mb-2" do %>
            Nombre del Almacén <span class="text-red-500">*</span>
          <% end %>
          <%= form.text_field :name, 
              required: true,
              class: "w-full px-4 py-2.5 border border-gray-300 rounded-lg focus:ring-2 focus:ring-teal-500 focus:border-transparent transition duration-200", 
              placeholder: "Ej. Almacén Central, Bodega Norte, Local Principal" %>
          <p class="mt-1.5 text-xs text-gray-500">
            Ingresa un nombre descriptivo para identificar este almacén
          </p>
        </div>

        <!-- Ubicación -->
        <div>
          <%= form.label :location, "Ubicación", class: "block text-sm font-medium text-gray-700 mb-2" do %>
            Ubicación <span class="text-red-500">*</span>
          <% end %>
          <%= form.text_field :location, 
              required: true,
              class: "w-full px-4 py-2.5 border border-gray-300 rounded-lg focus:ring-2 focus:ring-teal-500 focus:border-transparent transition duration-200", 
              placeholder: "Ej. Calle Principal #123, Col. Centro" %>
          <p class="mt-1.5 text-xs text-gray-500">
            Dirección física o descripción de la ubicación del almacén
          </p>
        </div>

        <!-- Info adicional -->
        <div class="bg-teal-50 border border-teal-200 rounded-lg p-4">
          <div class="flex items-start">
            <svg class="w-5 h-5 text-teal-600 mr-3 mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
            </svg>
            <div class="text-sm text-teal-800">
              <p class="font-semibold mb-1">Tip:</p>
              <p>Los almacenes te permiten organizar tu inventario por ubicaciones físicas. Puedes tener múltiples almacenes y gestionar el stock de cada producto en cada uno.</p>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Botones de acción -->
    <div class="flex flex-col sm:flex-row gap-3 pt-4">
      <%= form.submit @warehouse.new_record? ? "Crear Almacén" : "Guardar Cambios", 
          class: "flex-1 sm:flex-none inline-flex justify-center items-center px-6 py-3 border border-transparent text-base font-medium rounded-lg text-white bg-teal-600 hover:bg-teal-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-teal-500 shadow-md hover:shadow-lg transition duration-200" %>
      
      <%= link_to "Cancelar", 
          warehouses_path, 
          class: "flex-1 sm:flex-none inline-flex justify-center items-center px-6 py-3 border border-gray-300 text-base font-medium rounded-lg text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-teal-500 transition duration-200" %>
    </div>

  <% end %>
</div>