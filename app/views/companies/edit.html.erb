<div class="min-h-screen bg-gray-50">
  <div class="max-w-4xl mx-auto px-4 py-6">
    
    <!-- Header -->
    <div class="bg-gradient-to-r from-blue-600 to-blue-700 text-white rounded-lg shadow-md p-6 mb-6">
      <h1 class="text-3xl font-bold mb-2">⚙️ Configuración de Empresa</h1>
      <p class="text-blue-100">Esta información aparecerá en los tickets de venta</p>
    </div>

    <%= form_with model: @company, url: company_path, method: :patch, class: "space-y-6" do |f| %>
      
      <!-- Errores -->
      <% if @company.errors.any? %>
        <div class="bg-red-50 border-l-4 border-red-500 p-4 rounded-lg">
          <div class="flex items-start">
            <svg class="w-5 h-5 text-red-400 mt-0.5 mr-3" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"/>
            </svg>
            <div>
              <h3 class="text-red-800 font-semibold mb-2"><%= pluralize(@company.errors.count, "error") %> impidieron guardar:</h3>
              <ul class="list-disc list-inside text-red-700 text-sm space-y-1">
                <% @company.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          </div>
        </div>
      <% end %>

      <!-- Información Básica -->
      <div class="bg-white rounded-lg shadow-md overflow-hidden">
        <div class="bg-gradient-to-r from-gray-700 to-gray-800 px-6 py-4">
          <h2 class="text-xl font-bold text-white flex items-center gap-2">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"/>
            </svg>
            Información Básica
          </h2>
        </div>
        
        <div class="p-6">
          <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
            <!-- Nombre Comercial -->
            <div class="md:col-span-2">
              <%= f.label :business_name, "Nombre Comercial *", class: "block text-sm font-semibold text-gray-700 mb-1" %>
              <%= f.text_field :business_name, 
                placeholder: "Ejemplo: Tienda Don Pepe",
                class: "w-full px-3 py-2 text-base border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition",
                required: true %>
              <p class="text-xs text-gray-500 mt-1">Este nombre aparecerá en los tickets</p>
            </div>
            
            <!-- Razón Social -->
            <div class="md:col-span-2">
              <%= f.label :legal_name, "Razón Social", class: "block text-sm font-semibold text-gray-700 mb-1" %>
              <%= f.text_field :legal_name, 
                placeholder: "Opcional - Para facturación",
                class: "w-full px-3 py-2 text-base border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition" %>
            </div>
            
            <!-- RFC -->
            <div>
              <%= f.label :rfc, "RFC", class: "block text-sm font-semibold text-gray-700 mb-1" %>
              <%= f.text_field :rfc, 
                placeholder: "XAXX010101000",
                maxlength: 13,
                class: "w-full px-3 py-2 text-base font-mono border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition uppercase" %>
              <p class="text-xs text-gray-500 mt-1">13 caracteres</p>
            </div>
            
            <!-- Teléfono -->
            <div>
              <%= f.label :phone, "Teléfono", class: "block text-sm font-semibold text-gray-700 mb-1" %>
              <%= f.text_field :phone, 
                placeholder: "(771) 123-4567",
                class: "w-full px-3 py-2 text-base border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition" %>
            </div>
            
            <!-- Email -->
            <div class="md:col-span-2">
              <%= f.label :email, "Correo Electrónico", class: "block text-sm font-semibold text-gray-700 mb-1" %>
              <%= f.email_field :email, 
                placeholder: "contacto@tienda.com",
                class: "w-full px-3 py-2 text-base border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition" %>
            </div>
          </div>
        </div>
      </div>

      <!-- Dirección -->
      <div class="bg-white rounded-lg shadow-md overflow-hidden">
        <div class="bg-gradient-to-r from-green-600 to-green-700 px-6 py-4">
          <h2 class="text-xl font-bold text-white flex items-center gap-2">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
            </svg>
            Dirección
          </h2>
        </div>
        
        <div class="p-6 space-y-4">
          <!-- Dirección Completa -->
          <div>
            <%= f.label :address, "Dirección Completa", class: "block text-sm font-semibold text-gray-700 mb-1" %>
            <%= f.text_area :address, 
              placeholder: "Calle, número, colonia",
              rows: 2,
              class: "w-full px-3 py-2 text-base border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition resize-none" %>
          </div>
          
          <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
            <!-- Ciudad -->
            <div>
              <%= f.label :city, "Ciudad", class: "block text-sm font-semibold text-gray-700 mb-1" %>
              <%= f.text_field :city, 
                placeholder: "Zacualtipán",
                class: "w-full px-3 py-2 text-base border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition" %>
            </div>
            
            <!-- Estado -->
            <div>
              <%= f.label :state, "Estado", class: "block text-sm font-semibold text-gray-700 mb-1" %>
              <%= f.select :state, 
                options_for_select(Company::MEXICAN_STATES, @company.state),
                { include_blank: "Selecciona un estado" },
                class: "w-full px-3 py-2 text-base border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition" %>
            </div>
            
            <!-- Código Postal -->
            <div>
              <%= f.label :postal_code, "Código Postal", class: "block text-sm font-semibold text-gray-700 mb-1" %>
              <%= f.text_field :postal_code, 
                placeholder: "43200",
                maxlength: 5,
                class: "w-full px-3 py-2 text-base border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition" %>
              <p class="text-xs text-gray-500 mt-1">5 dígitos</p>
            </div>
          </div>
        </div>
      </div>

      <!-- Facturación (Opcional) -->
      <div class="bg-gradient-to-r from-purple-50 to-pink-50 border-2 border-dashed border-purple-300 rounded-lg overflow-hidden">
        <div class="bg-gradient-to-r from-purple-600 to-purple-700 px-6 py-4">
          <h2 class="text-xl font-bold text-white flex items-center gap-2">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
            </svg>
            Facturación Electrónica
          </h2>
        </div>
        
        <div class="p-6">
          <p class="text-sm text-purple-700 mb-4">
            <span class="font-semibold">Opcional</span> - Para futuras integraciones de facturación
          </p>
          
          <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
            <!-- Régimen Fiscal -->
            <div>
              <%= f.label :tax_regime, "Régimen Fiscal", class: "block text-sm font-semibold text-gray-700 mb-1" %>
              <%= f.select :tax_regime,
                options_for_select(Company::REGIMENES_FISCALES, @company.tax_regime),
                { include_blank: "Selecciona un régimen fiscal" },
                class: "w-full px-3 py-2 text-base border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent transition" %>
            </div>

            <!-- Uso de CFDI -->
            <div>
              <%= f.label :cfdi_use, "Uso de CFDI", class: "block text-sm font-semibold text-gray-700 mb-1" %>
              <%= f.select :cfdi_use,
                options_for_select(Company::USOS_CFDI, @company.cfdi_use),
                { include_blank: "Selecciona un uso de CFDI" },
                class: "w-full px-3 py-2 text-base border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent transition" %>
            </div>
          </div>
        </div>
      </div>

      <!-- Botones -->
      <div class="flex flex-col sm:flex-row gap-3">
        <%= f.submit "💾 Guardar Cambios", 
          class: "flex-1 px-6 py-3 bg-gradient-to-r from-blue-600 to-blue-700 text-white text-base font-bold rounded-lg shadow-lg hover:from-blue-700 hover:to-blue-800 transform hover:scale-[1.02] transition duration-200" %>
        
        <%= link_to "Cancelar", company_path, 
          class: "flex-1 text-center px-6 py-3 bg-gray-200 text-gray-700 text-base font-semibold rounded-lg hover:bg-gray-300 transition duration-200" %>
      </div>

      <!-- Nota -->
      <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
        <div class="flex items-start">
          <svg class="w-5 h-5 text-blue-500 mt-0.5 mr-3" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"/>
          </svg>
          <div>
            <p class="text-sm text-blue-800">
              <span class="font-semibold">* Campos requeridos</span><br>
              El resto de los campos son opcionales pero recomendados para tickets profesionales.
            </p>
          </div>
        </div>
      </div>

    <% end %>

  </div>
</div>