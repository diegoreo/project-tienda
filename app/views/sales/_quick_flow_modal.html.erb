<!-- Modal de autorización rápida -->
<div class="fixed inset-0 backdrop-blur-[1px] bg-black/10 z-50 flex items-center justify-center hidden"     data-quick-flow-target="modal"
     data-action="click->quick-flow#closeOnBackdrop keydown@window->quick-flow#handleKeydown">
  
  <div class="bg-white rounded-lg shadow-2xl max-w-md w-full mx-4 transform transition-all"
       onclick="event.stopPropagation()">
    
    <!-- Header -->
    <div class="bg-gradient-to-r from-green-600 to-green-700 text-white px-6 py-4 rounded-t-lg">
      <div class="flex items-center justify-between">
        <div class="flex items-center gap-3">
          <span class="text-5xl" data-quick-flow-target="modalIcon">💰</span>
          <h3 class="text-xl font-bold" data-quick-flow-target="modalTitle">Nuevo Movimiento</h3>
        </div>
        <button type="button"
                class="text-white hover:text-gray-200 transition"
                data-action="click->quick-flow#closeModal">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
          </svg>
        </button>
      </div>
    </div>

    <!-- Body -->
    <form data-action="submit->quick-flow#submit" class="p-6 space-y-4">
      
      <!-- Mensaje de error -->
      <div class="hidden bg-red-50 border-l-4 border-red-500 p-3 rounded"
           data-quick-flow-target="errorMessage">
        <p class="text-sm text-red-700 font-semibold"></p>
      </div>

      <!-- Tipo de movimiento (hidden) -->
      <input type="hidden" 
             data-quick-flow-target="movementType"
             value="">

      <!-- Monto -->
      <div>
        <label class="block text-sm font-semibold text-gray-700 mb-2">
          💵 Monto:
        </label>
        <div class="relative">
          <span class="absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-500 font-bold">$</span>
          <input type="number"
                 step="0.01"
                 min="0.01"
                 placeholder="0.00"
                 required
                 class="w-full pl-8 pr-4 py-3 text-lg font-bold border-2 border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-transparent"
                 data-quick-flow-target="amount">
        </div>
      </div>

      <!-- Descripción -->
      <div>
        <label class="block text-sm font-semibold text-gray-700 mb-2">
          📝 Descripción:
        </label>
        <textarea rows="3"
                  placeholder="Explica el motivo del movimiento (mínimo 5 caracteres)"
                  required
                  class="w-full px-4 py-2 border-2 border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-transparent resize-none"
                  data-quick-flow-target="description"></textarea>
        <p class="text-xs text-gray-500 mt-1">Mínimo 5 caracteres</p>
      </div>

      <!-- Separador -->
      <div class="border-t-2 border-gray-200 my-4"></div>

      <!-- Título de autorización -->
      <div class="bg-blue-50 border-l-4 border-blue-500 p-3 rounded">
        <p class="text-sm font-semibold text-blue-800 flex items-center gap-2">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/>
          </svg>
          Requiere autorización de supervisor
        </p>
      </div>

      <!-- Select de autorizadores -->
      <div>
        <label class="block text-sm font-semibold text-gray-700 mb-2">
          👤 Autorizar con:
        </label>
        <select required
                class="w-full px-4 py-2 border-2 border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-transparent"
                data-quick-flow-target="authorizerSelect">
          <option value="">Seleccionar supervisor...</option>
          <% @authorizers.each do |user| %>
            <option value="<%= user.id %>">
              <%= user.name %> (<%= user.role_name %>)
            </option>
          <% end %>
        </select>
      </div>

      <!-- Contraseña -->
      <div>
        <label class="block text-sm font-semibold text-gray-700 mb-2">
          🔑 Contraseña del supervisor:
        </label>
        <input type="password"
               placeholder="••••••••"
               required
               autocomplete="off"
               class="w-full px-4 py-2 border-2 border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-transparent"
               data-quick-flow-target="password">
        <p class="text-xs text-gray-500 mt-1">La contraseña del usuario seleccionado</p>
      </div>

      <!-- Botones -->
      <div class="flex gap-3 pt-4">
        <button type="button"
                class="flex-1 px-4 py-2 bg-gray-200 hover:bg-gray-300 text-gray-700 font-semibold rounded-lg transition"
                data-action="click->quick-flow#closeModal">
          ❌ Cancelar
        </button>
        
        <button type="submit"
                class="flex-1 px-4 py-2 bg-green-600 hover:bg-green-700 text-white font-bold rounded-lg shadow transition"
                data-quick-flow-target="submitButton">
          ✅ Autorizar y Registrar
        </button>
      </div>

    </form>

  </div>
</div>